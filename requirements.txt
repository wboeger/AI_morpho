Flask>=3.0
requests>=2.31
Flask-SQLAlchemy>=3.1
Flask-Login>=0.6
Flask-WTF>=1.2
Werkzeug>=3.0
numpy>=1.24
Pillow>=10.0
gunicorn>=21.2

# NOTE: torch / torchvision / opencv are only needed for offline U-Net landmark
# training (see unet/), NOT by the web app. They are intentionally excluded from
# the deploy build to keep it small/fast. Install them locally if training:
#   pip install torch torchvision opencv-python-headless
